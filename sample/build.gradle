apply plugin: 'com.android.application'

dependencies {
    compile project(':FlipView')
    compile 'com.android.support:support-v4:22.1.1'
}

android {
    compileSdkVersion 19
    buildToolsVersion '22.0.1'

  defaultConfig {
    applicationId "se.emilsjolander.flipview.demo"
    minSdkVersion 14
    targetSdkVersion 15
    versionCode 1
    versionName "1.0"
  }
}
